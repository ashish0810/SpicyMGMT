# SpicyMGMT

Run 'node index.js' to start the app. Then access by going to http://localhost:3000

If packages are not installed, run 'npm i'

You can change the port and files location in the .env file